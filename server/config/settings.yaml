# server/config/settings.yaml
# 配置文件 - 所有全局配置集中在此

database:
  host: "10.112.204.7"
  port: 3306
  username: "root"
  password: "Bupt@1234"
  name: "intent"
  pool_size: 10
  max_overflow: 20

auth:
  secret_key: "your-secret-key-change-in-production"
  algorithm: "HS256"
  access_token_expire_hours: 168  # 7天 = 24 * 7

app:
  host: "0.0.0.0"
  port: 6000
